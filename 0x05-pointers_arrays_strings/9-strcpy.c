#include "main.h"
/**
 * *_strcpy -  a function that copies the string pointed to by src
 * @dest: charcter input
 * @src: charcter input
 * Return: Always 0
 */
char *_strcpy(char *dest, char *src)
{
	int i = -i;

	do {
		i++;
		dest[i] = src[i];
	} while (src[i] != '\0');
	return (dest);
}
